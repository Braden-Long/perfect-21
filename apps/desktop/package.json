{
  "name": "@perfect21/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Perfect 21 desktop shell (macOS now, Windows later, Steam-ready)",
  "main": "main.cjs",
  "scripts": {
    "start": "npm run build -w @perfect21/game && electron .",
    "dist": "npm run build -w @perfect21/game && electron-builder --config electron-builder.json"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0"
  }
}
